version: '3.8'

services:
  api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    environment:
      - FLASK_APP=api/app.py
      - PYTHONUNBUFFERED=1
    command: gunicorn -w 4 -b 0.0.0.0:5000 api.app:app
